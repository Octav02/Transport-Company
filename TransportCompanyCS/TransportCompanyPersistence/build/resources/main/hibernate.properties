# MySQL
jakarta.persistence.jdbc.url=jdbc:mysql://localhost:3306/transport_company
jakarta.persistence.jdbc.driver=com.mysql.cj.jdbc.Driver

# Credentials
jakarta.persistence.jdbc.user=octav
jakarta.persistence.jdbc.password=16Ert53mIn

# Dialect
hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Schema Generation. Options are create, create-drop, validate, and update
jakarta.persistence.schema-generation.database.action=update

# SQL statement logging
hibernate.show_sql=true
hibernate.format_sql=true
hibernate.highlight_sql=true